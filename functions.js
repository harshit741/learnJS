let killTask = function(name){
    console.log(`Kill the person named ${name}`);
}
let name = 'Mark Dacasco';
killTask(name);

let missionCompleted = function(name){
    console.log(`Lethal action, target down. ${name} confirm kill`);
}
missionCompleted(name);